{
  "name": "lua-hr-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Bilingual HR agent on Lua for a multi-country GCC conglomerate — leave management and SOP requests, backed by BambooHR and Google Sheets.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run kb:check && npm run test && npm run smoke",
    "kb:build": "tsx scripts/build-kb.ts",
    "kb:check": "tsx scripts/build-kb.ts --check",
    "rules:status": "tsx scripts/rule-status.ts",
    "smoke": "tsx scripts/smoke.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "lua-cli": "^3.27.0"
  }
}
